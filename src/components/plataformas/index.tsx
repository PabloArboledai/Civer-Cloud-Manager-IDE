import React from 'react';

/**
 * Componente principal para Plataformas (Módulo exportable si se necesita usar fuera del Route)
 */
export const PlataformasIndex: React.FC = () => {
  return (
    <div className="flex flex-col gap-4">
      {/* Contenedor futuro para sub-componentes (DNS, Caché, Túneles) */}
    </div>
  );
};
